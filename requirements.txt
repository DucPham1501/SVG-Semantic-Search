sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
pandas>=1.5.0
numpy>=1.23.0
tqdm>=4.65.0
transformers>=4.40.0
torch>=2.0.0          # CPU-only build is fine: pip install torch --index-url https://download.pytorch.org/whl/cpu
sentencepiece>=0.1.99  # required by flan-t5 tokenizer
bitsandbytes>=0.43.0   # required for load_in_4bit=True (Phi-3-mini quantization)
accelerate>=1.1.0      # required by bitsandbytes 4-bit quantization
